import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
import { ProductCard } from "@/components/product-card";
import { fallbackProducts } from "@/lib/store-data";

export const Route = createFileRoute("/catalog")({ head: () => ({ meta: [{ title: "Каталог живых композиций — Калерия" }, { name: "description", content: "Флорариумы, моссариумы, панно из мха и бонсай ручной работы в Москве." }, { property: "og:title", content: "Каталог — Арт-студия Калерия" }, { property: "og:description", content: "Природные арт-объекты ручной работы." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: CatalogPage });
const filters = [["all", "Все"], ["florarium", "Флорариумы"], ["mossarium", "Моссариумы"], ["panel", "Панно"], ["circle", "Круги"], ["bonsai", "Бонсай"]];
function CatalogPage() { const [filter, setFilter] = useState("all"); const products = fallbackProducts.filter((p) => filter === "all" || p.category === filter); return <main className="page"><header className="page-head"><p className="eyebrow">Коллекция</p><h1>Природные объекты<br />для интерьера</h1><p>Каждая работа собирается вручную и немного отличается от фотографии.</p></header><div className="flex flex-wrap gap-2 border-y border-border py-4">{filters.map(([value,label]) => <button key={value} onClick={() => setFilter(value ?? "all")} className={`px-4 py-2 text-sm transition-colors ${filter === value ? "bg-primary text-primary-foreground" : "bg-secondary"}`}>{label}</button>)}</div><div className="grid gap-x-7 gap-y-14 py-14 sm:grid-cols-2 lg:grid-cols-3">{products.map((p) => <ProductCard key={p.id} product={p} />)}</div></main> }